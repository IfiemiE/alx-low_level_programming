#ifndef MAIN_H
#define MAIN_H
#include <unistd.h>
#include <stdlib.h>

void _putchar(char);
unsigned int binary_to_uint(const char *);
void print_binary(unsigned long int);

























#endif
