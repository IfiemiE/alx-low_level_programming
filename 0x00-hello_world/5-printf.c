#include <stdio.h>

/**
 * main - displays a string, followed by a formatted new line
 * Return: 0 to terminate program
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
