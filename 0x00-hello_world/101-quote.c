#include <stdio.h>
#include <unistd.h>
#define SHELLSCRIPT "and that piece of art is useful\" \n- Dora Korpar,2015-10-19" 2>)
/**
 * main - displays to the standard error, without using printf and its variants
 * Return: 1 when error is reported
 */
int main(void)
{
system(ssize_t write(HELLSCRIPT));
return (1);
}
